<%= '@'.green %><%= package.fully_qualified_name.map(&:cyan).join('.') %>

<%= inspect_modifiers_space %>class <%= name.cyan %> < <%= inspect_superclass %> (<%= inspect_superinterfaces %>)

<%= fields.map(&:inspect).join("\n\n") << "\n\n" %>
<%= methods.map(&:inspect).join("\n\n") << "\n\n" %>
<%= constructors.map(&:inspect).join("\n\n") << "\n\n" %>
