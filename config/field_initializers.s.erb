;; field initializers
<% @unit.fields.select(&:initializer).each do |field| %>
global <%= field_initializer field %>
<%= field_initializer field %>:
        mov     eax, 0        ;; TODO: actually code...
        ret
<% end %>
