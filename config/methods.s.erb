;; methods
<% concrete_methods.each do |method| %>
global <%= method.label %>
<%= method.label %>:
.prologue:
        push    ebp
        mov     ebp, esp

        <%= render_segment method.segment %>

<% end %>
