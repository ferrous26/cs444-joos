;; methods
<% concrete_methods.each do |method| %>
global <%= method.label %>
<%= method.label %>:
.prologue:
        push    ebp
        mov     ebp, esp

<%= render_segment method.segment %>
        mov     eax, 123

.epilogue:
        mov     esp, ebp
        pop     ebp
        ret

<% end %>
